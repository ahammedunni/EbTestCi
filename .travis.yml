
language: csharp

matrix:
  include:
    - os: linux # Ubuntu 16.04
      dist: trusty
      sudo: required
      mono: 5.4.1
      dotnet: 2.0.0

before_script:
  - chmod a+x ./build.sh
script:
  - ./build.sh
